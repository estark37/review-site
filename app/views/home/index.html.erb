<div class="navbar">
  <div class="navbar-inner">
    <div class="container">
      <%= link_to "Code Reviews", root_path, :class => "brand" %>
      <ul class="nav">
        <% if loggedin? %>
        <%= render "home_nav" %>
        <% else %>
        <%= render "splash_nav" %>
        <% end %>
      </ul>
    </div>
  </div>
</div>

<div class="container">

<% if loggedin? %>
<%= render "home" %>
<% else %>
<%= render "splash" %>
<% end %>

